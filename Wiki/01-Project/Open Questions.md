# Open Questions

#project #research

- Should `managing-feature-flags` eventually become a full loop skill, or stay a lifecycle protocol?
- Where should the iteration-board loop live — `using-forge/LOOP.md` or a separate skill?
- Do we need `skills/quality/checking-sub-slices/` as a dedicated inner-checker skill?
- Should desk-check states (`ready-for-deskcheck`, `in-deskcheck`) be visible in Linear or only in loop-state files?
- How do we represent human gates (`awaiting-human-gate-N`) in handoff graphs without dead ends?
